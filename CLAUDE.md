# [Claude Code 작업 지시서] 해외영업팀 업무 자동화 — 클라이언트 미팅용 딜러 대시보드 + 팀 업무 통합 포털

- 기획자: 최율아 (Padlet 게시자명 "제발", 첨부 파일 `업무자동화_요청서_최율아.md`)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 웹 포털(6개 기능) + 엑셀 업로드 기반 딜러 대시보드

---

## 0. Claude Code에게 — 작업 방식

1. 기획자가 이미 **우선순위 3단계**를 제안했다. 그 순서를 따른다: **1단계(주간업무 자동 알림, 경과채권 바로가기) → 2단계(출장 일정, 팀회의 일정, 환율 입력) → 3단계(지역별 매출 추이 그래프, 딜러 미팅 대시보드)**.
2. 기획서 각 섹션의 "전문가에게 확인이 필요한 사항"이 곧 9번 질문이다. 작업 전 사용자에게 묻고, 답이 없으면 5번 가정으로 진행한다.
3. 포털은 **한 화면에서 6개 기능에 접근**하는 구조다. 공통 레이아웃(네비게이션)을 먼저 만들고 기능을 하나씩 채운다.
4. 출장 중 모바일 접속이 많을 것으로 예상되므로 **반응형**을 기본으로 한다.
5. 부록의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

14개 해외 딜러를 관리하는 해외영업팀을 위해, ① 엑셀 업로드만으로 딜러별 미팅 자료가 만들어지는 **클라이언트 미팅 대시보드**와 ② 주간업무·경과채권·지역별 매출·환율·출장일정·팀회의를 한 화면에서 관리하는 **팀 업무 통합 포털**을 만든다.

## 2. 배경 및 현재 업무 상황

- 해외영업팀, 14개 딜러 관리, 잦은 해외 출장.
- 주간업무는 매주 메일로 공지, 환율은 담당자가 매월 메일로 업데이트, 경과채권 파일은 'Efam' 공유 폴더에서 매번 찾아야 함.
- 팀 운영 업무가 메일·공유폴더·스프레드시트에 흩어져 있어 통합 화면이 필요.

## 3. 요구 기능

### A. 클라이언트 미팅용 대시보드 (3단계)
- 엑셀 양식에 정보 입력 → 업로드 → 대시보드 자동 반영.
- **14개 딜러별 개별 조회/전환**(딜러 선택 드롭다운 또는 탭).
- 표시 항목(확인 필요): 매출, 재고, 채권, 계약 조건 등.
- 출력 형태(확인 필요): 웹 페이지 / PPT 자동 생성 / PDF 리포트.
- 오프라인 사용 가능성(비행기·인터넷 미연결 지역) 고려 → 정적 HTML 내보내기 또는 PWA 캐시.
- 딜러별 과거 추이(히스토리) 표시 여부 확인.

### B. 팀 업무 통합 포털 — 6개 기능

| # | 기능 | 단계 | 요구사항 |
|---|---|---|---|
| B-1 | 주간업무작성 | 1 | 매주 메일 공지를 대체. 작성 화면 + **자동 메일/알림 발송**. 데이터는 구글 스프레드시트(공유) 기반 |
| B-2 | 경과채권(연체/미수) | 1 | 'Efam' 공유 폴더에서 파일 찾는 번거로움 제거. **클릭 한 번으로 폴더 이동 또는 파일 바로 열람** |
| B-3 | 지역별 매출 추이 | 3 | 주간업무 데이터를 반영해 자동 생성. 모든 법인 + 직수출 지역의 월간/주간 매출 그래프 |
| B-4 | 매월 환율 공지 | 2 | 담당자가 메일로 보내던 환율을 **화면에서 직접 입력/수정**, 전 팀원 조회 |
| B-5 | 출장 일정 공유 | 2 | 출장 일정 등록·공유, **캘린더 형태 권장** |
| B-6 | 팀회의 일정 | 2 | 팀회의 일정 등록 및 전 팀원 조회 |

## 4. 데이터 스키마 (샘플 데이터 생성 지침)

- **딜러(Dealer)**: 딜러코드, 딜러명, 국가/지역, 담당자, 통화
- **딜러 실적(DealerMetrics)**: 딜러코드, 기준월, 매출, 재고, 채권잔액, 경과채권, 계약조건 요약 (엑셀 업로드 양식과 1:1)
- **주간업무(WeeklyReport)**: 주차, 작성자, 지역/법인, 주요 업무, 매출(주간), 이슈, 다음주 계획
- **경과채권 링크(ReceivableLink)**: 법인/딜러, 파일명, 경로(URL 또는 폴더 경로), 갱신일
- **환율(ExchangeRate)**: 기준월, 통화, 환율, 입력자, 입력일
- **출장 일정(Trip)**: 팀원, 국가/도시, 시작일, 종료일, 목적, 방문 딜러
- **팀회의(Meeting)**: 일시, 장소/링크, 안건, 참석자
- **사용자(User)**: 이름, 이메일, 역할(팀원/책임자)

샘플: 딜러 14개(실명 대신 가상명), 12개월 실적, 주간업무 8주치, 출장·회의 일정 각 10건.

## 5. 제약사항 및 가정

- 구글 스프레드시트 연동이 허용된다는 가정(사내 보안 정책 확인 필요). 불허 시 Supabase/로컬 DB로 대체 가능하도록 데이터 계층 분리.
- 'Efam' 폴더는 사내 서버(네트워크 드라이브)일 가능성 → 1차는 **경로 링크 목록 관리(file:// 또는 UNC 경로 복사)**, 클라우드(구글드라이브)라면 직접 열기 링크.
- 자동 메일 수신자 목록은 화면에서 편집 가능하게(고정 목록보다 유연).
- 권한: 팀원은 본인 작성분 수정, 책임자는 전체 수정 — 단순 역할 2단계로 가정.
- 모바일 조회 필수 가정.

## 6. 권장 기술 스택 (제안)

- 프론트: Next.js(React) 또는 단일 HTML/JS 멀티페이지. 캘린더는 FullCalendar.
- 데이터: 구글 스프레드시트(Google Sheets API / Apps Script) 또는 Supabase. 1단계 검증은 Apps Script 웹앱이 가장 빠를 수 있음.
- 메일 발송: Apps Script MailApp / Gmail API 또는 이메일 API.
- 차트: Chart.js / ECharts.
- 딜러 대시보드 오프라인: 정적 HTML 내보내기(엑셀 → JSON 임베드) 또는 PWA.
- PPT/PDF 자동 생성 선택 시: PptxGenJS / 브라우저 인쇄.

## 7. 산출물

1. 통합 포털 웹앱(6개 기능) + 반응형 레이아웃
2. 딜러 미팅 대시보드 + 엑셀 업로드 양식(템플릿 파일)
3. 샘플 데이터(스프레드시트 또는 시드)
4. 자동 메일/알림 모듈
5. README(배포, 스프레드시트 연결, 수신자 관리, Efam 링크 등록 방법)

## 8. 구현 단계

- Phase 1 (1단계): 포털 레이아웃 + 주간업무 작성·목록·자동 알림 + 경과채권 바로가기 목록
- Phase 2 (2단계): 출장 일정 캘린더 + 팀회의 일정 + 환율 입력/조회
- Phase 3 (3단계): 지역별 매출 추이 그래프(주간업무 데이터 집계) + 딜러 미팅 대시보드(엑셀 업로드 → 14개 딜러 전환)
- Phase 4: 오프라인 내보내기, 권한, 모바일 마감, README

## 9. 확인 필요 사항 (작업 전 질문 — 기획서의 "전문가에게 확인이 필요한 사항" 그대로)

**대시보드**
1. 엑셀에 입력할 정보 항목(매출, 재고, 채권, 계약 조건 등) 구체화
2. 대시보드 형태: 웹 페이지 / PPT 자동 생성 / PDF 리포트 중 선호
3. 오프라인(비행기, 인터넷 미연결 지역) 사용 필요 여부
4. 딜러별 데이터 히스토리(과거 추이) 표시 필요 여부

**포털**
5. 팀 규모(사용자 수) 및 권한 구분 필요 여부(팀원/책임자별 수정 권한 차등)
6. 'Efam' 폴더가 사내 서버인지 클라우드(구글드라이브 등)인지
7. 자동 메일 발송 시 수신자 목록 관리 방식(고정 vs 화면에서 편집)
8. 사내 보안 정책상 외부 서비스(구글 워크스페이스 등) 연동 허용 여부
9. 모바일에서도 조회 가능해야 하는지 여부

---

## 부록. 기획서 원문 (첨부 `업무자동화_요청서_최율아.md` 전문)

# 업무 자동화 요청서

## 요청 배경
해외 영업팀에서 14개 딜러를 관리하며, 잦은 해외 출장과 반복되는 팀 운영 업무를 자동화·효율화하고자 합니다. 아래 두 가지 시스템 구축을 요청드립니다.

---

## 1. 클라이언트 미팅용 대시보드

### 목적
출장 시 딜러 미팅에 활용할 자료를 반자동으로 생성

### 핵심 기능
- 엑셀 양식에 정보 입력 → 업로드 → 대시보드에 자동 반영
- 14개 딜러별 데이터를 개별 조회/전환 가능해야 함

### 전문가에게 확인이 필요한 사항
- 엑셀에 입력할 정보 항목(예: 매출, 재고, 채권, 계약 조건 등) 구체화
- 대시보드 형태: 웹 페이지 / PPT 자동 생성 / PDF 리포트 중 선호 방식
- 오프라인(비행기, 인터넷 미연결 지역) 사용 필요 여부
- 딜러별 데이터 히스토리(과거 추이) 표시 필요 여부

---

## 2. 팀 업무 통합 관리 화면

한 화면(포털)에서 아래 6개 기능을 관리

### 2-1. 주간업무작성
- 매주 메일로 공지하던 방식을 대체
- 자동 메일 또는 알림 발송 기능 추가
- 관리는 구글 스프레드시트(공유 파일) 기반

### 2-2. 경과채권 (연체/미수 채권)
- 매번 'Efam' 공유 폴더에서 파일을 찾는 번거로움 제거
- 화면에서 클릭 한 번으로 해당 폴더 위치 이동 또는 파일 바로 열람

### 2-3. 지역별 매출 추이
- 주간업무 데이터를 반영하여 자동 생성
- 모든 법인 + 직수출 지역의 월간/주간 매출을 그래프로 표시

### 2-4. 매월 환율 공지
- 기존: 담당자가 매월 메일로 환율 업데이트
- 개선: 화면에서 직접 입력/수정 가능하도록 변경

### 2-5. 출장 일정 공유
- 해외 영업팀 특성상 잦은 해외 출장 일정을 팀원 간 공유
- 출장 일정 등록 화면 (캘린더 형태 권장)

### 2-6. 팀회의 일정
- 팀회의 일정 등록 및 전 팀원 조회 가능한 페이지

### 전문가에게 확인이 필요한 사항
- 팀 규모(사용자 수) 및 권한 구분 필요 여부(예: 팀원/책임자별 수정 권한 차등)
- 'Efam' 폴더가 사내 서버인지 클라우드(구글드라이브 등)인지 → 연동 방식이 달라짐
- 자동 메일 발송 시 수신자 목록 관리 방식(고정 vs 화면에서 편집)
- 사내 보안 정책상 외부 서비스(구글 워크스페이스 등) 연동이 허용되는지 여부
- 모바일에서도 조회 가능해야 하는지 여부(출장 중 접속 빈도가 높을 것으로 예상)

---

## 참고: 우선순위 제안 (선택 사항)
전문가와 논의 시 아래와 같이 단계별 진행을 제안하면 견적/일정 협의가 수월할 수 있습니다.
1. 1단계: 주간업무작성 자동 알림, 경과채권 바로가기 (구현 난이도 낮음, 즉각적 번거로움 해소)
2. 2단계: 출장 일정 공유, 팀회의 일정, 환율 입력 화면
3. 3단계: 지역별 매출 추이 그래프, 클라이언트 미팅용 대시보드 (데이터 연동 복잡도 높음)
